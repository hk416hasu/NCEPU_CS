#include <stdio.h>

int main() {
    // 这只能输出1普通字符与2回车符的ascii码
    // int c = 0;
    // while ((c = getchar()) != EOF) {
    //     printf("%d\n", c);
    // }

    printf("the value of EOF: %x\n", EOF);

    return 0;
}
